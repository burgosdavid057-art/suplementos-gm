<?php
/**
 * Wrapper común para emails. Recibe:
 *   $headline string  — bloque grande arriba
 *   $intro    string  — párrafo bajo el headline (HTML permitido)
 *   $bodyHtml string  — bloque central HTML (tabla de items, etc.)
 *   $cta      ?array  — ['label'=>'Pagar','url'=>'...']
 *   $footer   ?string — pie con info adicional
 *   $accent   ?string — color hex del header (default verde marca)
 *
 * Email-safe HTML: tablas anidadas, estilos inline, sin display:flex.
 * Funciona en Gmail/Outlook/Apple Mail.
 */
$accent  = $accent  ?? '#28b22e';
$footer  = $footer  ?? null;
$cta     = $cta     ?? null;
$logoUrl = (env('APP_URL') ?: 'https://suplementosequinosgm.co') . '/assets/logo-gm.png';
?><!DOCTYPE html>
<html lang="es">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width,initial-scale=1">
<title><?= e($headline ?? 'Suplementos GM') ?></title>
</head>
<body style="margin:0;padding:0;background:#f5f6f7;font-family:-apple-system,BlinkMacSystemFont,'Segoe UI',Roboto,Helvetica,Arial,sans-serif;color:#2d3436;">
  <table role="presentation" width="100%" cellpadding="0" cellspacing="0" border="0" style="background:#f5f6f7;padding:28px 12px;">
    <tr>
      <td align="center">
        <table role="presentation" width="600" cellpadding="0" cellspacing="0" border="0" style="max-width:600px;width:100%;background:#ffffff;border-radius:14px;overflow:hidden;box-shadow:0 2px 14px rgba(0,0,0,0.05);">

          <!-- Header con logo + barra de color -->
          <tr>
            <td style="background:<?= e($accent) ?>;padding:22px 28px;">
              <table width="100%" cellpadding="0" cellspacing="0" border="0">
                <tr>
                  <td style="vertical-align:middle;">
                    <img src="<?= e($logoUrl) ?>" alt="Suplementos GM" width="44" height="44" style="display:inline-block;vertical-align:middle;border:0;">
                    <span style="display:inline-block;vertical-align:middle;margin-left:10px;font-size:18px;font-weight:800;color:#ffffff;letter-spacing:-0.01em;">
                      Suplementos <span style="color:#c6ffdb;">GM</span>
                    </span>
                  </td>
                </tr>
              </table>
            </td>
          </tr>

          <!-- Headline -->
          <tr>
            <td style="padding:28px 28px 8px 28px;">
              <h1 style="margin:0;font-size:22px;line-height:1.25;color:#1a1d1f;font-weight:800;letter-spacing:-0.02em;">
                <?= $headline ?? '' ?>
              </h1>
              <?php if (!empty($intro)): ?>
                <p style="margin:10px 0 0 0;font-size:15px;line-height:1.55;color:#525a5e;">
                  <?= $intro ?>
                </p>
              <?php endif; ?>
            </td>
          </tr>

          <!-- Body HTML -->
          <?php if (!empty($bodyHtml)): ?>
          <tr>
            <td style="padding:18px 28px 8px 28px;">
              <?= $bodyHtml ?>
            </td>
          </tr>
          <?php endif; ?>

          <!-- CTA -->
          <?php if ($cta && !empty($cta['url']) && !empty($cta['label'])): ?>
          <tr>
            <td style="padding:18px 28px 24px 28px;">
              <table role="presentation" cellpadding="0" cellspacing="0" border="0">
                <tr>
                  <td style="background:<?= e($accent) ?>;border-radius:8px;">
                    <a href="<?= e($cta['url']) ?>"
                       style="display:inline-block;padding:13px 26px;font-size:15px;font-weight:700;color:#ffffff;text-decoration:none;border-radius:8px;">
                      <?= e($cta['label']) ?> →
                    </a>
                  </td>
                </tr>
              </table>
            </td>
          </tr>
          <?php endif; ?>

          <!-- Footer -->
          <tr>
            <td style="padding:18px 28px 26px 28px;border-top:1px solid #ececec;">
              <p style="margin:0;font-size:12px;line-height:1.55;color:#8a929a;">
                <?php if ($footer): ?>
                  <?= $footer ?><br><br>
                <?php endif; ?>
                ¿Dudas? Escríbenos por WhatsApp al
                <a href="https://wa.me/573105043520" style="color:#28b22e;text-decoration:none;font-weight:600;">+57 310 504 3520</a>
                o responde a este correo.
              </p>
              <p style="margin:14px 0 0 0;font-size:11px;color:#b8bec3;">
                Suplementos GM · Rionegro, Antioquia, Colombia<br>
                <a href="<?= e(env('APP_URL') ?: 'https://suplementosequinosgm.co') ?>" style="color:#b8bec3;text-decoration:underline;">suplementosequinosgm.co</a>
              </p>
            </td>
          </tr>
        </table>
      </td>
    </tr>
  </table>
</body>
</html>
