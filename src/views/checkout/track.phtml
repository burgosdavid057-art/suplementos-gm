<?php
/**
 * Página de rastreo: el cliente entra el número de pedido + email,
 * y lo redirigimos a /checkout/orden/{numero}.
 *
 * Vars:
 *   $error  ?string
 *   $old    array  (numero, email)
 */
$error = $error ?? null;
$old   = $old   ?? ['numero' => '', 'email' => ''];
?>

<section class="relative overflow-hidden text-white"
         style="background:linear-gradient(135deg,#1d8e23 0%, #156c1a 50%, #28b22e 100%);">
  <div aria-hidden="true" class="absolute -left-32 top-0 h-80 w-80 rounded-full pointer-events-none"
       style="background:rgba(122,232,130,.20);filter:blur(80px);"></div>
  <div aria-hidden="true" class="absolute -right-24 bottom-0 h-80 w-80 rounded-full pointer-events-none"
       style="background:rgba(255,255,255,.10);filter:blur(80px);"></div>

  <div class="relative mx-auto max-w-3xl px-5 sm:px-6 py-12 sm:py-16 text-center">
    <div class="inline-flex items-center gap-2 rounded-full bg-white/15 backdrop-blur px-3 py-1 text-xs font-bold uppercase tracking-widest text-white border border-white/25">
      <span class="inline-block w-1.5 h-1.5 rounded-full bg-white animate-pulse"></span>
      Estado de tu pedido
    </div>
    <h1 class="mt-4 text-3xl sm:text-4xl md:text-5xl font-black tracking-tight">
      Rastrea tu pedido
    </h1>
    <p class="mt-3 max-w-xl mx-auto text-sm sm:text-base text-white/90">
      Ingresa tu <strong>número de pedido</strong> y el <strong>correo</strong> con el que compraste para ver el estado, los productos y el envío.
    </p>
  </div>
</section>

<section class="max-w-xl mx-auto px-5 sm:px-6 -mt-8 mb-12 relative z-10">
  <form method="post" action="/orden" class="rounded-2xl border border-silver-200 bg-white p-6 sm:p-7 shadow-xl shadow-brand-700/10">
    <?= csrf_field() ?>

    <?php if ($error): ?>
      <div class="mb-5 flex items-start gap-2 rounded-lg border border-red-200 bg-red-50 px-3.5 py-2.5 text-sm text-red-900">
        <span class="text-base leading-none mt-px">⚠</span>
        <span><?= e($error) ?></span>
      </div>
    <?php endif; ?>

    <label class="block text-xs font-bold uppercase tracking-wider text-carbon-soft mb-1.5" for="numero">
      Número de pedido
    </label>
    <input id="numero" name="numero" type="text" autocomplete="off" autofocus required
           value="<?= e($old['numero'] ?? '') ?>"
           placeholder="GM-2024-XXXXXX"
           class="w-full rounded-lg border border-silver-200 px-3.5 py-2.5 font-mono uppercase tracking-wide focus:border-brand-500 focus:outline-none focus:ring-2 focus:ring-brand-500/20">
    <p class="mt-1 text-xs text-carbon-soft">
      Te lo enviamos por correo cuando hiciste la compra. También está en la URL de la confirmación.
    </p>

    <label class="mt-5 block text-xs font-bold uppercase tracking-wider text-carbon-soft mb-1.5" for="email">
      Email del comprador
    </label>
    <input id="email" name="email" type="email" autocomplete="email" required
           value="<?= e($old['email'] ?? '') ?>"
           placeholder="tu@correo.com"
           class="w-full rounded-lg border border-silver-200 px-3.5 py-2.5 focus:border-brand-500 focus:outline-none focus:ring-2 focus:ring-brand-500/20">

    <button class="mt-6 w-full h-11 rounded-md bg-brand-500 hover:bg-brand-600 text-white font-bold transition shadow-md shadow-brand-500/30">
      Ver estado del pedido →
    </button>

    <p class="mt-5 text-center text-xs text-carbon-soft">
      ¿No encuentras tu número?
      <a href="https://wa.me/573105043520?text=Hola%2C+no+encuentro+mi+n%C3%BAmero+de+pedido"
         target="_blank" rel="noopener"
         class="font-semibold text-brand-600 hover:underline">Escríbenos por WhatsApp</a>
    </p>
  </form>

  <!-- Mini-tarjetas de ayuda -->
  <div class="mt-8 grid grid-cols-1 sm:grid-cols-2 gap-3 text-sm">
    <div class="rounded-xl border border-silver-200 bg-white p-4">
      <p class="font-bold text-carbon inline-flex items-center gap-2"><?= icon('mail', 'w-4 h-4 text-brand-600') ?> Revisa tu correo</p>
      <p class="mt-1 text-xs text-carbon-soft">El número aparece en el asunto del email de confirmación que te enviamos.</p>
    </div>
    <div class="rounded-xl border border-silver-200 bg-white p-4">
      <p class="font-bold text-carbon inline-flex items-center gap-2"><?= icon('refresh', 'w-4 h-4 text-brand-600') ?> Guarda el link</p>
      <p class="mt-1 text-xs text-carbon-soft">La página de tu pedido se actualiza sola cuando entra el pago.</p>
    </div>
  </div>
</section>
