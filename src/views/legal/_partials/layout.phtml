<?php
/**
 * Partial: wrapper común para las 4 páginas legales.
 * Imprime el hero, la sidebar de navegación y abre/cierra el contenedor.
 * Cada vista legal incluye este partial antes y después del contenido.
 *
 * Vars:
 *   $pageTitle, $lastUpdated, $legal, $pages, $currentSlug
 *   $contentOpen (bool) — true al abrir, no se incluye al cerrar
 */
?>
<section class="w-full bg-gradient-to-br from-carbon via-carbon to-brand-900 text-white">
  <div class="w-full max-w-5xl mx-auto px-4 sm:px-6 py-10 sm:py-14">
    <nav class="text-[11px] sm:text-xs text-white/60 mb-2 flex flex-wrap items-center gap-x-1">
      <a href="/" class="hover:text-brand-300">Inicio</a>
      <span>/</span>
      <span class="text-white">Legal</span>
    </nav>
    <h1 class="text-2xl sm:text-3xl md:text-4xl font-bold"><?= e($pageTitle) ?></h1>
    <p class="mt-2 text-xs sm:text-sm text-white/70">
      Última actualización: <?= e($lastUpdated) ?>
    </p>
  </div>
</section>

<div class="w-full max-w-5xl mx-auto px-4 sm:px-6 py-8 sm:py-12 flex flex-col lg:flex-row gap-6 lg:gap-10">

  <!-- Sidebar de navegación entre páginas legales -->
  <aside class="w-full lg:w-[220px] lg:flex-shrink-0 lg:sticky lg:top-24 self-start">
    <nav class="rounded-xl border border-silver-200 bg-white p-2 text-sm">
      <?php foreach ($pages as $slug => $info): ?>
        <a href="/legal/<?= e($slug) ?>"
           class="block px-3 py-2 rounded-md transition <?= $slug === $currentSlug ? 'bg-brand-50 text-brand-700 font-semibold' : 'text-carbon hover:bg-silver' ?>">
          <?= e($info['title']) ?>
        </a>
      <?php endforeach; ?>
    </nav>

    <div class="mt-4 rounded-xl border border-silver-200 bg-white p-4 text-xs text-carbon-soft space-y-1.5">
      <p class="font-bold text-carbon"><?= e($legal['name']) ?></p>
      <p><?= e($legal['doc_type']) ?>: <?= e($legal['doc_id']) ?></p>
      <p><?= e($legal['address']) ?></p>
      <p>
        <a href="mailto:<?= e($legal['email']) ?>" class="text-brand-700 hover:underline"><?= e($legal['email']) ?></a>
      </p>
      <p>
        <a href="<?= e($legal['whatsapp']) ?>" target="_blank" rel="noopener" class="text-brand-700 hover:underline"><?= e($legal['phone']) ?></a>
      </p>
    </div>
  </aside>

  <!-- Contenido legal -->
  <article class="flex-1 min-w-0 rounded-2xl border border-silver-200 bg-white p-5 sm:p-8 lg:p-10 legal-prose">
